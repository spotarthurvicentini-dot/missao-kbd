# CONFIGURAÇÃO DO GITHUB

`_config-legado.yml` é a configuração usada pela publicação antiga baseada na raiz do repositório. Ela foi preservada apenas como histórico e não participa mais do deploy.

A publicação atual é definida em `.github/workflows/deploy-pages.yml` e usa `SITE/publicacao/` como origem.
